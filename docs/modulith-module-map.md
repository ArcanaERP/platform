# Arcana ERP Modulith Module Map

Updated: 2026-04-18

## Scope

This map covers the currently implemented Spring Modulith modules under `com.arcanaerp.platform`:

- `core`
- `identity`
- `products`
- `orders`
- `invoicing`
- `payments`
- `agreements`
- `inventory`
- `communicationevents`

## Dependency Graph

Consumer -> allowed dependency

- `identity` -> `core::pagination`
- `products` -> `core::pagination`, `identity`
- `orders` -> `core::pagination`, `products`
- `invoicing` -> `core::pagination`, `orders`
- `payments` -> `identity`, `invoicing`
- `agreements` -> `core::pagination`, `identity`
- `inventory` -> `core::pagination`, `core::errors`
- `communicationevents` -> `core::pagination`, `identity`

Notes:

- `core::pagination` is a named interface exported from `com.arcanaerp.platform.core.pagination`.
- `core::errors` is a named interface exported from `com.arcanaerp.platform.core.api`.
- No module is allowed to depend on another module's `.internal` package.

## Module Boundaries

| Module | Package | Responsibilities | Public API (examples) | HTTP Surface |
| --- | --- | --- | --- | --- |
| Core | `com.arcanaerp.platform.core` | Shared primitives and cross-cutting infrastructure plus foundational units-of-measurement catalog support (API errors, UTC clock, pagination, UOM directory) | `PageQuery`, `PageResult` (via `core::pagination`), `UnitOfMeasurementDirectory` | `POST /api/core/units-of-measurement`, `GET /api/core/units-of-measurement` |
| Identity | `com.arcanaerp.platform.identity` | Explicit tenant registration, tenant updates, tenant directory and direct tenant lookup, tenant/role/user primitives, direct user lookup, filterable user directory reads, user updates, explicit role registration, direct role lookup, role updates, role directory, org-unit directory, cross-module actor lookup | `TenantDirectory`, `UserDirectory`, `RoleDirectory`, `OrgUnitDirectory`, `IdentityActorLookup` | `POST /api/identity/tenants`, `GET /api/identity/tenants`, `GET /api/identity/tenants/{code}`, `PATCH /api/identity/tenants/{code}`, `POST /api/identity/users`, `GET /api/identity/users/{userId}`, `PATCH /api/identity/users/{userId}`, `GET /api/identity/users`, `POST /api/identity/roles`, `GET /api/identity/roles/{code}`, `PATCH /api/identity/roles/{code}`, `GET /api/identity/roles`, `POST /api/identity/org-units`, `GET /api/identity/org-units/{code}`, `PATCH /api/identity/org-units/{code}`, `GET /api/identity/org-units` |
| Products | `com.arcanaerp.platform.products` | Product catalog, activation lifecycle, activation audit history, orderability lookup | `ProductCatalog`, `ProductLookup` | `POST /api/products`, `GET /api/products`, `PATCH /api/products/{sku}/active`, `GET /api/products/{sku}/activation-history` |
| Orders | `com.arcanaerp.platform.orders` | Sales order creation, listing, and status transitions | `OrderManagement` | `POST /api/orders`, `GET /api/orders`, `PATCH /api/orders/{orderNumber}/status` |
| Invoicing | `com.arcanaerp.platform.invoicing` | Order-backed invoice creation, direct retrieval, filtered paged listing, minimal lifecycle transitions (`DRAFT -> ISSUED`, `DRAFT/ISSUED -> VOID`; `VOID` final), and immutable status-change history | `InvoiceManagement` | `POST /api/invoices`, `GET /api/invoices/{invoiceNumber}`, `GET /api/invoices`, `PATCH /api/invoices/{invoiceNumber}/status`, `GET /api/invoices/{invoiceNumber}/status-history` |
| Payments | `com.arcanaerp.platform.payments` | Payment capture against issued invoices plus invoice balance, tenant receivables read models, tenant receivables summary totals, tenant receivables aging buckets, per-bucket drill-down, over-90 collections queue reads with due-date cutoff, sortable focused over-90 assignee workload rollups, filterable and sortable current assignee dashboard rollups plus daily, weekly, and monthly dashboard trend reads, unassigned over-90 queue shortcuts, unassigned over-90 summary totals, claim-based intake from the unassigned queue with append-only per-invoice and tenant-wide claim history plus daily, weekly, and monthly claim rollups, release back to the unassigned queue with append-only per-invoice and tenant-wide release history plus daily, weekly, and monthly release rollups, net intake rollups by actor plus sortable assignee operations rollups and daily, weekly, and monthly actor trends across claim and release audits, assignee triage, sortable assignee-plus-aging current-state workload rollups, follow-up-date filtering, scheduled-vs-unscheduled follow-up filtering, latest follow-up-outcome filtering, latest follow-up-outcome portfolio summaries, filterable and sortable current-portfolio assignee-plus-latest-outcome rollups, assignee-by-outcome and actor-by-outcome follow-up rollups, daily, weekly, and monthly actor-plus-outcome follow-up completion summaries, daily/weekly/monthly follow-up completion-outcome summaries, and optional follow-up-driven prioritization, assignment workflow for queue ownership, follow-up scheduling and completion on assigned collections work with completion outcomes and per-invoice follow-up history, append-only invoice notes/worklog with tenant-wide feed reads and assignee-scoped filtering, structured category/outcome metadata, outcome-based and category-based note summaries with assignee-scoped filtering, daily/weekly/monthly note activity summaries, daily/weekly/monthly category note trend summaries, daily/weekly/monthly collections note category-outcome cross-tab summaries, daily/weekly/monthly note outcome trend summaries, per-invoice and tenant-level assignment history with assignee/date filters, current workload summary by assignee, daily/weekly/monthly assignment activity trends, paged reconciliation reads, tenant-scoped collection summary, invoice-level tenant breakdown, and UTC day/week/month-bucketed tenant trends | `PaymentManagement` | `POST /api/payments`, `GET /api/payments/invoices/{invoiceNumber}/balance`, `GET /api/payments/tenants/{tenantCode}/receivables`, `GET /api/payments/tenants/{tenantCode}/receivables/summary`, `GET /api/payments/tenants/{tenantCode}/receivables/aging`, `GET /api/payments/tenants/{tenantCode}/receivables/aging/{agingBucket}`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/assignee-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-dashboard-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-dashboard/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-dashboard/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-dashboard/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/unassigned`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/unassigned/summary`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/claim`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/release`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/claim-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/release-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/claim-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/claims/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/claims/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/claims/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/release-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/releases/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/releases/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/releases/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/net-intake/actor-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-operations-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/net-intake/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/net-intake/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/net-intake/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/current-assignee-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/assignee-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/actor-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/actor/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/actor/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/actor/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/follow-up-outcome/monthly-summary`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/assignment`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/follow-up`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/follow-up/complete`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/follow-up-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/assignment-history`, `POST /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/notes`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/over-90/{invoiceNumber}/notes`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/outcome-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category-outcome/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category-outcome/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/category-outcome/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/outcome/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/outcome/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/notes/outcome/monthly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignment-history`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/assignee-aging-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/daily-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/receivables/collections/monthly-summary`, `GET /api/payments`, `GET /api/payments/tenants/{tenantCode}/summary`, `GET /api/payments/tenants/{tenantCode}/invoices`, `GET /api/payments/tenants/{tenantCode}/daily-summary`, `GET /api/payments/tenants/{tenantCode}/weekly-summary`, `GET /api/payments/tenants/{tenantCode}/monthly-summary` |
| Agreements | `com.arcanaerp.platform.agreements` | Agreement master-record creation, direct retrieval by agreement number, paged listing (optional status filtering), lifecycle transitions (`DRAFT -> ACTIVE`, `DRAFT/ACTIVE -> TERMINATED`; `TERMINATED` final), and immutable status-change history with tenant-scoped actor/reason attribution and optional audit filters | `AgreementManagement` | `POST /api/agreements`, `GET /api/agreements/{agreementNumber}`, `GET /api/agreements`, `PATCH /api/agreements/{agreementNumber}/status`, `GET /api/agreements/{agreementNumber}/status-history` |
| Inventory | `com.arcanaerp.platform.inventory` | On-hand inventory by `sku + location`, location-scoped adjustment transactions, and location-to-location transfers with optional source-document references and idempotent reversal retries with payload-consistency, concurrent first-write conflict enforcement, and stale-claim recovery | `InventoryAvailability` | `GET /api/inventory/{sku}`, `POST /api/inventory/{sku}/adjustments`, `GET /api/inventory/{sku}/adjustments`, `POST /api/inventory/{sku}/transfers`, `GET /api/inventory/transfers/{transferId}`, `POST /api/inventory/transfers/{transferId}/reversals`, `GET /api/inventory/transfers/{transferId}/reversals`, `GET /api/inventory/{sku}/transfers` |
| Communication Events | `com.arcanaerp.platform.communicationevents` | Tenant-scoped communication event logging with actor validation through identity, direct lookup by event number, and filterable paged directory reads by channel, direction, and recording actor | `CommunicationEventLog` | `POST /api/communication-events`, `GET /api/communication-events/{eventNumber}`, `GET /api/communication-events` |

## Boundary Rules In Use

- Root module packages expose contracts (`*Command`, `*View`, service interfaces).
- Persistence/domain implementations stay in `.internal`.
- Cross-module calls are made only through exported module APIs:
  - `products` -> `identity` via `IdentityActorLookup`
  - `agreements` -> `identity` via `IdentityActorLookup`
  - `orders` -> `products` via `ProductLookup`
  - `invoicing` -> `orders` via `OrderManagement`
  - `payments` -> `invoicing` via `InvoiceManagement`
  - `payments` -> `identity` via `IdentityActorLookup`
  - `communicationevents` -> `identity` via `IdentityActorLookup`
- Shared paging contract is centralized in `core::pagination`.
